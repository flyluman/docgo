version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - gocritic
    - misspell
    - revive
    - unconvert
    - bodyclose
    - copyloopvar

issues:
  max-issues-per-linter: 0
  max-same-issues: 0